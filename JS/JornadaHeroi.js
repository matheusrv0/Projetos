const posInicial = parseInt(prompt("Digite a posição inicial do herói: "));
const totalPassos = parseInt(prompt("Digite o total de passos que o herói deu: "));

const posFinal = posInicial + totalPassos

print("A posição final do herói é " + posFinal);